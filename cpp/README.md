# C++ implementation

Reference implementation. Being moved here from the node repository: `libzbc` (keys, addresses,
transaction serialisation, signing, HTTP client) plus one program per transaction and `zbc-cli`.
Builds with CMake, no dependency on the node.
